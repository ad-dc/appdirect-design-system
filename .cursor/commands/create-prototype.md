Start a self-serve prototype. Prefer the GitHub template.

Read and follow `.cursor/skills/start-prototype/SKILL.md`.

If the user explicitly wants a local copy from this design-system checkout instead, follow `.cursor/skills/create-prototype/SKILL.md`.
