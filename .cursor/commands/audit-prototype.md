Follow the audit-prototype skill in `.cursor/skills/audit-prototype/SKILL.md`.

Run `npm run ds:audit` on this repo. Restricted `@mantine/core` imports fail. Missing `PageContentHeader` and handmade record lists warn.
